#include <stdio.h>

/**
 * main - This is the point of entry
 *
 * Print "with proper grammar, but the outcome is a piece of art,"
 *
 * Return: It's always 0 (showing success)
 */

int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");

	return (0);
}
